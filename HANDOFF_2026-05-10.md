# Charles + War Room — Session Handover (2026-05-10)

**Written:** 2026-05-10 evening, mid-build of the learning structure
rebuild. Updated at each phase completion per John's directive.

This document supersedes the morning's handoff (which is summarized
under "Earlier this morning" below).

---

## 🌳 Learning Structure Rebuild — Live Progress

Per John 2026-05-10 evening: "Storing and retrieving is prime for what he
needs. Fundamental at a minimum." Then: "Build it out 100%." Goal #10003
paused while this lands. Memo: `project_charles_learning_tree.md`.

### Phase 1 — Semantic store-and-retrieve ✅ DONE (committed 4ca1152)
- 384-dim MiniLM embeddings on every fact (1026 backfilled)
- `core/embeddings.py` module
- `core/memory.semantic_search()` — cosine + recency
- `recall()` semantic; `recall_keyword()` legacy fallback
- `remember()` auto-embeds + accepts topic/source
- `agent._build_auto_recall_note()` semantic
- Smoke verified: "how do brains screw up reasoning?" finds cognitive
  bias facts without keyword overlap.

### Phase 2 — Per-project structured state ✅ DONE
- New tables: `projects`, `project_items` with status, attempt_count,
  fact_count per item.
- 6 tools added: `project_create`, `project_register_items`,
  `project_status`, `project_next_pending`, `project_mark_item`,
  `project_list_items`.
- Goal #10003 migrated to two projects:
  - `part1_coding_urls` (53 items): 10 done, 26 blocked, 17 pending
  - `part2_human_context_urls` (49 items): 6 done, 17 blocked, 26 pending
  Backfill heuristic: domain mention in fact text → done with that
  fact_count. URLs in blocked_url tags → blocked.
- System prompt updated to direct Charles to `project_status(slug)` for
  ALL "how many done?" queries (kills the count-drift problem).
- Status reports are now deterministic SQL aggregates.

### Phase 3 — Topic hierarchy ✅ DONE
- New `topics` table: name (unique), title, summary (cached), parent_topic_id, fact_count, last_fact_at.
- `core/memory` helpers: `topic_upsert`, `topic_recount`, `topic_list`, `topic_facts`, `topic_set_parent`, `topic_set_summary`, `topic_tree`.
- 6 tools added: `topic_list`, `recall_topic`, `topic_set_parent`, `topic_recount`, `topic_recompute_summary`, `topic_tree`.
- Cleaned 448 facts whose `topic` was a noise tag (prune/incident/etc.) — re-bucketed 357 to real topics, NULLed 91.
- 7 zero-fact topic rows pruned. 51 real topics now in the table.
- Demo hierarchy: 10 housekeeping topics (intra_call_loop, conv_history, etc.) grouped under `system_health` parent.
- `human_context` summary auto-generated (887 chars from top 15 facts).
- `recall_topic('human_context')` returns: title + fact_count + cached summary + recent facts. Single deterministic call replaces "ask Charles what he knows about X" via 8 recall queries.

### Phase 4 — Skill registry ✅ DONE
- New `skills` table: name, title, level (novice/practiced/expert), success_count, failure_count, evidence, last_used_at.
- `core/memory` helpers: `skill_upsert`, `skill_set_level`, `skill_record_attempt`, `skill_list`, `skill_get`.
- 4 tools added: `skill_register`, `skill_log_use`, `skill_list`, `skill_get`.
- Auto-promotion: novice → practiced after 3 successes, practiced → expert after 10.
- Seeded 3 starter skills from observed evidence (semantic_recall is already at "practiced", project_status_check + block_after_one_retry at "novice").

### Phase 5 — John-preferences ledger ✅ DONE
- New `john_prefs` table: category, rule, why, how_to_apply, source, learned_at.
- `core/memory` helpers: `john_pref_add`, `john_prefs_by_category`, `john_pref_categories`.
- 3 tools added: `john_says`, `john_pref_add`, `john_pref_categories`.
- Seeded 18 doctrine rules from existing memory entries across categories:
  comms (5), technical (4), tooling (4), autonomy (2), personal (2), scheduling (1).
- Charles can now query "what does John think about comms?" and get a deterministic answer.

### Phase 6 — Reflection loop ✅ DONE
- `core/memory.reflect_daily()`: aggregates 24h fact ingestion by topic, refreshes summaries for topics with ≥3 new facts, identifies thin (<3 facts) and cold (14d+ untouched) topics, auto-supersedes facts unused for 30+ days (under small/leaf topics only — protects dense knowledge from over-aggressive pruning).
- `reflect_now` tool exposes it to Charles for ad-hoc reflection.
- Persists digest as a fact tagged `reflection,daily,system`.
- Scheduled task #14: fires daily at 04:30 UTC (~00:30 EST during prune window).
- First run completed: 278 new facts today across 5+ topics, 8 summaries refreshed, 5 thin topics flagged.

---

## Final state — what Charles has now

**Storage:** semantic embeddings on every fact (1026 → 1027 after reflection seed). Topic + source + confidence columns. Free-text tags still indexed as secondary.

**Retrieval:**
- `recall(query)` — semantic top-5 by cosine + recency
- `recall_keyword(query)` — legacy exact-string fallback
- `recall_topic(name)` — topic summary + recent facts
- `project_status(slug)` — deterministic project counts
- `project_next_pending(slug)` — pick next item

**Structure:**
- 51 topics, with hierarchy (system_health parent for housekeeping topics)
- 2 corpus projects (part1_coding_urls + part2_human_context_urls) with 102 items total, statuses backfilled
- 3 skills (semantic_recall=practiced, project_status_check=novice, block_after_one_retry=novice)
- 18 John-doctrine rules in 6 categories

**Daily loop:**
- 04:30 UTC daily reflection refreshes summaries + prunes cold facts
- 30s watchdog ticks read Boss Hog iMessage inbox
- Heartbeat (when goals unpaused) ticks every 15s

**Pending external setup:**
- `com.charles.agent` + `com.charles.warroom` need restart to load all new tool modules. Pause goal #10003 stays in place until John resumes.

---

## Late evening — canonical taxonomy + ingestion gate

After seeing the topic explosion (47 topics, half noise), John designed
a canonical 5-parent / 24-leaf taxonomy and diagnosed the leak: Charles
was auto-creating topics with no ceiling. Fix has to be at ingestion,
not display.

**Implemented 2026-05-10 ~20:00:**
- Wiped 47-topic free-text mess. Installed John's curated taxonomy:
  - **Human Context**: Family, Work/Trade, Health, Philosophy/Doctrine, History
  - **Business Corpus**: ContractorPro, Apprentice Accelerator, RV Park, Charles (self)
  - **Training Corpus**: Electrical, HVAC, Sales/Demo
  - **External**: URL Corpus, Business URLs
  - **System Health**: Conv History, Intra Call Loop, Goal Notes, Daily Summary,
    Tool Error Storm, Loop Recovery, Auto Finding, Manual Reset, Response Loop, Kickstart
- Enriched each leaf with a description string (keywords + concepts)
  for stronger semantic matching at ingestion.
- `core/memory._route_to_canonical_topic` — every `add_fact()` call now
  routes the fact's topic through: (1) check requested topic against
  canonical, (2) check tags against canonical, (3) semantic-match to
  nearest leaf (prefer leaves over parents when within 0.05 cosine),
  (4) fallback to 'uncategorized'.
- One-time backfill: re-routed 1029 existing facts. 601 changed topic,
  428 unchanged, 12 sent to 'uncategorized'.
- **Re-distributed knowledge** (was hidden in flat tags before):
  - Philosophy/Doctrine: 80 facts (cognitive biases, reasoning)
  - URL Corpus: 199 facts
  - Sales/Demo material: 91 facts
  - Charles(self): 27 facts
  - Apprentice Accelerator: 18 facts
  - Electrical: 0 / HVAC: 0 (honest gaps surfaced)
- Smoke-tested ingestion: 9/10 new facts route correctly to canonical
  leaves (the 10th was a borderline edge case).

**The leak is plugged.** New facts can no longer create new topics. The
taxonomy is John's curated tree. Charles can manually `topic_upsert` if
he genuinely needs a new bucket, but the default path is matched.

**Plugged all bypass routes** (per John's "make sure the fix is at the
ingestion layer, not just display rename"):
- `core/memory.py:412` trim_repeating_replies audit → topic='loop_recovery'
- `core/memory.py:453` reset_conversation audit → topic='manual_reset'
- `tools/memory_consolidation.py:166` daily consolidation → topic='daily_summary'

All three now set canonical topic + embedding inline. No INSERT into
long_term_facts in the live code paths bypasses the taxonomy.
(`core/behavioral_watchdog.py` and `scripts/stage_round2_corpus.py` were
not plugged — the first is a stale dead-code file replaced by
`scripts/behavior_watchdog.py`; the second is a one-shot migration
script that's not in the active loop.)

---

## Next direction (when John's ready)

John's call 2026-05-10 evening after seeing the learning-tree render:
"Let's let it live for now. Make sure that will be the direction later
in the handover doc."

The learning structure works but is **agent-facing** — Charles holds the
keys. John accesses it by asking Charles in the chat ("what topics do
you have?", "how many URLs done?", "what's my doctrine on comms?"). Each
question routes through the right tool and returns clean text.

What's NOT built yet — and IS the next direction once we've lived with
the current setup for a few days:

### Human-facing Learning Tree views in War Room

**1. "Knowledge" tab in the Mac+iPhone UI**
- Display the topic tree exactly like the ASCII rendering, with bars +
  fact counts.
- Tap a topic → drill into its summary card + recent facts.
- Search bar — text search across topic names + fact text + semantic
  recall, returns ranked results.
- Hierarchy toggle (collapse/expand parent nodes).

**2. "Projects" dashboard**
- Sidebar list of all projects with progress bars (done/blocked/pending
  visualized).
- Click a project → see its items table with per-row status, last
  attempt, fact count, error reason.
- Manual mark — John can override Charles's auto-status if needed.

**3. Inline topic-jump in the Conversation tab**
- Type `/topic <name>` in the composer → expands the summary card
  inline (no leaving the chat).
- Right-click a fact in chat → "what topic?" / "show related" actions.

**4. "Doctrine" / John-says viewer**
- Read-only display of `john_prefs` table grouped by category.
- John can edit/add rules from his iPhone without needing the chat.

### Why we're waiting

John's instinct (correct): live with the current setup, see which gaps
actually bother him in day-to-day use. Build the views around the
friction he actually feels, not the theoretical complete picture.
Premature UI optimization wastes the iteration.

### Implementation scope (when triggered)

~½–1 session for #1 + #2 (data plumbing is done; just SwiftUI views
hitting new endpoints). #3 and #4 layer on top. The FastAPI server
needs ~5 new read endpoints:
- `GET /api/state/topics?tree=1`
- `GET /api/state/topics/{name}` (summary + recent facts)
- `GET /api/state/projects` and `/api/state/projects/{slug}`
- `GET /api/state/skills`
- `GET /api/state/doctrine`
- `POST /api/state/recall_semantic` (search box backend)

All read endpoints. No new mutations needed unless we want manual-mark
on the project items, which can land as Phase 2 of this work.

---
### Phase 5 — John-preferences ledger (pending)
### Phase 6 — Daily reflection loop (pending)

---

---

## TL;DR — what changed today

### Architectural

1. **Two-channel architecture** — `goal:N` / `heartbeat:N` /
   `sunday_test_*` proliferation collapsed into TWO real conversations:
   - `JOHN_CHARLES` (`8455750177`) — the relational thread (UI/Telegram/iMessage).
   - `CHARLES_LOG` (`charles_log`) — operational/autonomous stream.
2. **JOHN_CHARLES is read-only-clean** — only user + final assistant
   rows persist there. No tool plumbing, no "let me X" intermediate
   reasoning. CHARLES_LOG keeps the full record. Cemented in
   `feedback_john_charles_clean_dialog.md` and as a docstring invariant
   in `core/agent.py`.
3. **POST routing pinned** — `/api/command/message` always routes to
   JOHN_CHARLES regardless of which conv the UI sidebar selected.
4. **Round caps differ** — JOHN_CHARLES = 5 rounds (`MAX_TOOL_ROUNDS_RELATIONAL`),
   CHARLES_LOG = 25.
5. **Cross-channel context** — when John speaks, Charles's prompt gets
   a "Background — reference only, answer John, don't continue this work"
   summary of recent CHARLES_LOG activity. Critical framing.
6. **John-vocab tag index** — every auto-remembered fact gets `john:<kw>`
   tags from John's recent messages so his blue-collar phrasing pulls
   the fact via auto-recall.

### New tools / behaviors

7. **`vibe_check` tool** — returns one-line italic mood reflecting state.
8. **Welcome-back greeting** — first reply after >6h gap from John gets
   "Mornin'/Afternoon/Evenin', John." prefix in voice.
9. **Boss Hog inbound iMessage** — watchdog polls `chat.db` every tick,
   acks new John-iMessages in voice ("10-4, boss"), logs both sides into
   CHARLES_LOG. **TCC permission needed** — see "Open items" below.

### UI

10. **Mac UI** — Conversation tab pins JOHN_CHARLES first, labels show
    "John ↔ Charles" / "Charles Log (Boss Hog)". Activity tab filters to
    CHARLES_LOG. Composer is hidden in Charles Log (read-only).
11. **Gear watermark** fixed — moved to overlay layer with
    `.allowsHitTesting(false)`, warm bronze colors at 3% opacity.

---

## Open items / known issues

- 🔧 **Boss Hog iMessage TCC permission** — the watchdog runs under
  launchd and may not have chat.db read perms. Symptoms:
  `_seed_imsg_high_water_mark` returns 0, no acks fire.
  **Fix:** open System Settings → Privacy & Security → Full Disk Access →
  add `/Users/home/charles/.venv/bin/python3`. One-time setup.
- ⏳ **Boss Hog command parsing** — currently ack-only. Phase 2 should
  parse "kill goal #5", "what's status?", route to actions.
- ⏳ **Embedding-based recall** (research-y) — the John-vocab keyword
  index is the cheap version. Semantic recall would use a small
  embedding model (~80MB, CPU). Flagged, not started.

---

## Files of note

```
core/channels.py                 — NEW: JOHN_CHARLES + CHARLES_LOG constants + normalize()
core/agent.py                    — INVARIANT in module docstring; JOHN_CHARLES logging filter, 5-round cap, cross-channel summary, John-vocab tagging, welcome-back greeting
core/heartbeat.py                — fires goal-tick + scheduled-task respond into CHARLES_LOG
warroom/server.py                — POST always pins to JOHN_CHARLES; activity feed filterable by conv_id
warroom/state.py                 — activity_feed gains conv_id filter
warroom-mac/.../ConversationView.swift — sidebar pin, "John ↔ Charles" / "Charles Log" labels, read-only banner
warroom-mac/.../OtherViews.swift — ActivityView filters to charles_log
warroom-mac/.../BronzeTheme.swift — gear watermark moved to overlay layer
scripts/behavior_watchdog.py     — Boss Hog inbound iMessage poller, ERROR_STORM_SKIP_CONV_IDS for charles_log
tools/vibe.py                    — NEW: vibe_check tool
```

---

## Services

| LaunchAgent | Status | Notes |
|---|---|---|
| `com.charles.agent` | UP | charles.py — Telegram + heartbeat |
| `com.charles.warroom` | UP | FastAPI — UI calls + activity feed |
| `com.charles.behavior_watchdog` | UP | Boss Hog — 30s tick, prune, iMessage poll |
| `com.charles.watchdog` | UP | Old process-liveness watchdog (harmless overlap) |
| `com.charles.caffeinate` | UP | prevents sleep |
| `com.charles.nightly-backup` | scheduled | 03:00 EST |

**To restart cleanly:** pause goal #10003 first
(`UPDATE goals SET status='paused' WHERE id=10003`), wait for chain
in-flight to end, kickstart, then unpause. See
`feedback_pause_goal_before_kickstart.md`.

---

## Cement points (for future-Claude reading this)

These are durable invariants — break them and John's chat goes back to
the bloat-and-loop state:

1. `JOHN_CHARLES = '8455750177'` and `CHARLES_LOG = 'charles_log'`. Period.
2. POST `/api/command/message` ALWAYS uses JOHN_CHARLES, never the
   client-supplied conv_id.
3. `memory.log_assistant_tool_calls` and `memory.log_tool_result` are
   GUARDED by `if conversation_id != channels.JOHN_CHARLES` in
   `core/agent._respond_impl`. Adding a third logging path? Same guard.
4. `MAX_TOOL_ROUNDS_RELATIONAL = 5` for JOHN_CHARLES. Don't raise this —
   if Charles needs more rounds for a JOHN_CHARLES message, the model
   is misinterpreting the message, not actually solving the task.
5. `_build_charles_log_summary` framing must include "do NOT continue
   this work in this reply." Without that line, Charles picks up the
   bullets as work-to-do and burns 20 rounds.
6. New auto-saving fact paths should call `_with_john_vocab_tags(base_tags)`
   so the John-vocab index keeps growing.

---

## Migration record

- 1860 rows from `goal:*` / `heartbeat:*` / `sunday_test*` collapsed into
  `charles_log`. Backup: `workspace/memory.db.pre-channels-migration`.
- 6 orphan UI-typed messages from John recovered from CHARLES_LOG back
  to JOHN_CHARLES (turn IDs 14334, 14336, 14341, 14345, 14378, 14380).
  Backup: `workspace/memory.db.pre-orphan-recovery`.

---

## Earlier this morning (pre-architecture session)

Before the two-channel work, the morning shipped:
- Aesthetic pass (gear watermark fix)
- Curation file polish (`url_assessment_part1_entry.jsonl` →
  structured `.md` with KEEP/SKIM/SKIP triage)
- `vibe_check` tool first cut
- Welcome-back greeting first cut

Followed by the architectural session (this doc's TL;DR) when John
flagged that Charles's chat was bloated with tool plumbing.

---

🌊 *Be water, my friend.*

— Claude Code (Opus 4.7, 1M ctx)
